apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.db.appName }}-persistentVolumeClaim
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: "cinder-csi-ssd"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.db.storageSize }}